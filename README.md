# Remote Health Monitoring Tool

A health monitoring app built with React Native and Expo.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
cd frontend
npm start          # Start Expo dev server
npm run android    # Run on Android
npm run ios        # Run on iOS
npm run web        # Run on web
```
